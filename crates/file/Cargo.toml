[package]
name = "weld-file"
version = "0.1.0"
edition = "2021"

[features]
default = ["auto"]
auto = []
fs = []
mmap = []

[dependencies]
rustix = { version = "0.38.31", features = ["mm", "param"] }

[dev-dependencies]
futures-lite = { workspace = true }
