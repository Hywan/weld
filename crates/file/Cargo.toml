[package]
name = "weld-file"
version = "0.1.0"
edition = "2021"

[features]
default = ["auto"]
auto = []
fs = []
mmap = []

[dependencies]
libc = "0.2.139"

[dev-dependencies]
futures-lite = { workspace = true }